name: dependency_font
description: "A test app using a font defined in a dependency package"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  spot:
    path: ../../../../../.
  app_font:
    path: packages/app_font


dev_dependencies:
  test: ^1.24.0
