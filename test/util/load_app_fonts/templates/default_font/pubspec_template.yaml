name: default_font
description: "An test app using the default font"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  spot:
    path: ../../../../../.

dev_dependencies:
  test: ^1.24.0
